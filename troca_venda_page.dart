import 'package:flutter/material.dart';

class TrocaVendaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Troca e Venda')),
      body: Center(child: const Text('Funcionalidade em desenvolvimento...')),
    );
  }
}
